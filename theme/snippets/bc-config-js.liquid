<script type="text/javascript">
  
  var _bc_config = {
    "enable_multiple_currencies" 	: "{% if settings.currency_type == '2' %}true{% else %}false{% endif %}"
    ,"money_format" 				: '{{ shop.money_format }}'
    ,"enable_title_blance"			: "{{ settings.enable_title_blance }}"
    ,"enable_rtl"				: "{{ settings.enable_rtl }}"
  };
    
</script>